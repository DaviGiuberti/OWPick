{
    "version": "1.2.3",
    "download_url": "https://github.com/DaviGiuberti/OWPick/releases/download/v1.2.3/OWPick_v1.2.3.zip",
    "notas": " ### Enemy Threat: curva re-ancorada em dois pontos escolhidos - A **forma** da curva do multiplicador de ameaça introduzida na v1.2.2 (`w(raw) = exp( A · tanh(raw / S) )`) **não muda**. O que muda é **como `A` e `S` são obtidos**: em vez de calibrados por percentil sobre a distribuição real de `raw` (`THREAT_CAP = 2.5`, `THREAT_SCALE = 2.5` fixados diretamente), a curva agora é *"
}
